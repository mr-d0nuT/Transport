name = "transport-bcn-relay"
main = "worker.js"
compatibility_date = "2026-09-01"

# La clave de la API de AMB NO va aquí (este fichero es público): se guarda
# como secreto con  npx wrangler secret put AMB_API_KEY
